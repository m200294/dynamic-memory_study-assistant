[project]
name = "generic-study-pipeline"
version = "0.1.0"
description = "A portable file-based study memory and mastery tracking pipeline"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.1.29",
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",
    "tzdata>=2024.1",
]

[tool.ruff]
line-length = 100
